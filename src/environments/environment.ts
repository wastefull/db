// The current environment may overwrite these values during the build cycle.

export const environment = {
  production: false,
  workspace: '/workspaces/waste',
  apiBaseUrl: 'http://localhost:8000',
};
