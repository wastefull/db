import { Component } from '@angular/core';

@Component({
  selector: 'app-default-router',
  template: '<!-- Default primary outlet -->',
  standalone: true,
})
export class DefaultRouterComponent {}
